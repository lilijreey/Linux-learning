glibc 提供的其他几个malloc控制，查询方法

* malloc(3), malloc_hook(3), malloc_info(3), malloc_stats(3), malloc_trim(3)
* mallopt  mallinfo(3) 
* mcheck(3), mtrace(3), posix_memalign(3) mmap(2), sbrk(2), 


几种流行的高效第三方malloc

+ jemalloc
   radis 使用jemalloc

+ tcmalloc  : Thread-Caching malloc

+ nedmalloc

+ dlmalloc

malloc + fork 父子各自有自己的malloc

性能比较：
  jemalloc 和 tcmalloc 比dlmalloc 好很多
  jemalloc 和 tcmalloc 的性能差不多

valgrind 检测不出tcmalloc 和 jemalloc的内存泄漏
  tcmalloc 可以使用gprof
